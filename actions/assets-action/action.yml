name: "Upload Assets to Release"
description: "Archive a folder (zip/tar/tar.gz) and upload it to a GitHub Release using the gh CLI, with retries and overwrite-on-conflict."
inputs:
  tag:
    description: "Git tag of the release"
    required: true
  item-path:
    description: "Path to the folder containing files to upload"
    required: true
  archive-type:
    description: "Type of archive to create (zip, tar, etc.)"
    required: false
    default: "zip"
  compression-level:
    description: "Compression level for the archive (0-9)"
    required: false
    default: "9"
  retries:
    description: "How many times to retry upload on failure"
    required: false
    default: "3"
  retry-delay-ms:
    description: "Delay between retries in milliseconds"
    required: false
    default: "1000"
  factor:
    description: "Exponential backoff factor for retries"
    required: false
    default: "1"
runs:
  using: "node24"
  main: "dist/index.js"